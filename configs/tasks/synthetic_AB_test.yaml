# Synthetic A→B test (mirrors organ→muscle structure with synthetic data)
# Used for pipeline validation only — no real data needed
# Uses different class counts per task to test multi-head output
task_sequence:
  name: synthetic_AB_test
  setting: task_incremental

  tasks:
    - id: taskA_organs
      description: "Simulated organ task (synthetic, 15 classes)"
      model:
        out_channels: 15
      data:
        synthetic:
          num_classes: 15

    - id: taskB_muscles
      description: "Simulated muscle task (synthetic, 11 classes)"
      model:
        out_channels: 11
      data:
        synthetic:
          num_classes: 11
