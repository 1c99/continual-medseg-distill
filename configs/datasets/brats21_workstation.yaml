data:
  source: brats21
  brats21:
    root: /media/user/data2/data2/data/Brats21
    layout: flat
    shape: [128, 128, 128]
    normalize_per_channel: true
    split_manifest: data/splits/brats21_smoke.json

model:
  in_channels: 4
  out_channels: 4
